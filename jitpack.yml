jdk:
  - openjdk11
before_install:
  - sdk install java 11.0.12-open
  - sdk use java 11.0.12-open
install:
  - ./gradlew clean build :coralogixlogger:publishToMavenLocal -x test -x lint --no-daemon --stacktrace